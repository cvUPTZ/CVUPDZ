{
  "name": "cvupdz",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd server && npm run start\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install && cd ../server && npm install"
  },
  
  "devDependencies": {
    "concurrently": "^6.2.0"
  },
  "engines": {
    "node": "18.x"
  }
}
